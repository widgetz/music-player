export default function (){
    const store = []; // problems with map colliding keys and array suffers same problem
                      //

    //todo: data would need to have a name
    function add(data){
        store.push(data);
    }

    function get(name){

    }


}
